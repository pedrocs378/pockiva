{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum capability set for the local desktop emulator",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "allow-foundation-status",
    "allow-emulator-runtime",
    "dialog:allow-open",
    "store:default"
  ]
}
